{
  "Parameters" : {
    "pEnvironment"    : "{{ environment }}",
    "pBucketName"     : "{{ bucket_name }}",
    "pBucketPath"     : "{{ bucket_path }}",
    "pHostedZone"     : "{{ hosted_zone }}",
    "pSubDomain"      : "{{ sub_domain }}",
    "pSslCertificate" : "{{ ssl_certificate }}"
  },
  "Tags" : {
    "ALAgroup"    : "api docs portal",
    "component"   : "docs",
    "environment" : "{{ environment }}",
    "branch"      : "{{ branch }}",
    "commit_id"   : "{{ commit_id }}",
    "build"       : "{{ build }}"
  },
  "StackPolicy" : {
    "Statement" : [
      {
        "Effect" : "Allow",
        "Action" : "Update:*",
        "Principal": "*",
        "Resource" : "*"
      }
    ]
  }
}
